const config = {
  screens: {
    Home: 'home',
    Profile: 'profile',
    Notifictaions: 'notifications',
    Settings: 'settings',
  },
};

const linking = {
  prefixes: ['mfg-ev://app'],
  config,
};

export default linking;
